// Constants for InfoTooltip component

// Viewport breakpoint for mobile/touch detection
export const MOBILE_BREAKPOINT = 768;

// Animation delay for tooltip hover (in milliseconds)
// This delay ensures smooth animation transitions and prevents
// the tooltip from appearing too quickly on accidental hovers
export const ANIMATION_DELAY = 200;
